<div class="container">
<h1>Persons</h1>
<hr>
<%= link_to 'Create', new_person_path %>
<hr>
<table class="table table-hover">
  <thead>
	<tr>
		<td>#</td>				
		<td>Name</td>
		<td>Phone</td>
		<td>Email</td>
		<td>Avatar</td>
		<td></td>		
	</tr>
	</thead>
  <tbody>
	<% @users.each do |user| %>
	<tr>
		<td><%= user.id %></td>				
		<td><%= user.name %></td>
		<td><%= user.phone %></td>
		<td><%= user.email %></td>
		<td><%= user.avatar %></td>
		<td>
		<%= link_to 'Edit', {:controller => "person", :action => "edit", :id => user.id, :organization_id => user.organization_id }  %>
		<%# <%= link_to 'Delete', per,:controller: , method: :delete, data: {confirm: "Are you sure wamt to delete this Persons?" } %>
		</td>		 
	<% end %>
	</tr>
	</tbody>
</table>
</div>
